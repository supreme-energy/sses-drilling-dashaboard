import React from "react";
import { useSelectedSurvey } from "../../selectors";

export default function FormationsTops({ container, width, formationsData }) {
  console.log("formationsData", formationsData);

  return null; //formationsData.map(formation => )
}
